import { StrikeDirective } from './strike.directive';

describe('StrikeDirective', () => {
  it('should create an instance', () => {
    const directive = new StrikeDirective();
    expect(directive).toBeTruthy();
  });
});
